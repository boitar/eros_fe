### 🐞 Bug 修复

- 修复"瀑布流-大"模式下画廊条目回滑时跳位/换列/重排的问题：条目不再经过 keframe 占位符（占位高度与真实高度不一致导致瀑布流列簿记失真），首帧即按真实封面比例布局
- 加载更多时复用同一列表对象追加数据，避免列表整体重建

---

### 🐞 Bug Fixes

- Fix gallery items jumping / switching columns / reordering when scrolling back in "waterfall large" mode: items no longer go through the keframe placeholder (whose height differs from the real one and broke the waterfall column bookkeeping) and are laid out at the real cover ratio on the first frame
- Reuse the same list object when loading more pages to avoid rebuilding the whole list
